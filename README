1) Extract flex_sdk_4.1 to flex/
2) The flowplayer distribution in flowplayer/ is setup to build the stw plugin already
3) Init the stw submodule: git submodule init
4) Setup your build.properties paths in flowplayer and flowplayer.devkit
